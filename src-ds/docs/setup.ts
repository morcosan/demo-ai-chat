import '@ds/release/setup'
import './styles.css'

LOG('BUILD_MODE:', ENV__BUILD_MODE)
LOG('BUILD_NUMBER:', ENV__BUILD_NUMBER)

// export function createStory(component: unknown, props?: unknown): void {
//
// }
